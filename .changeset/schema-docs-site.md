---
'@chronicle.app/schema': minor
---

Replace the single-page HTML reference with a documentation site built from the ontology, with guides, example records, and search. The package no longer ships `docs/schema.html` or exports `@chronicle.app/schema/schema.html`.

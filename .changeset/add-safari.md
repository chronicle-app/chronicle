---
'@chronicle.app/safari': minor
'@chronicle.app/schema': minor
'@chronicle.app/cli': minor
---

Add the Safari plugin, bundled with the CLI. It reads browsing history from Safari's `History.db` and emits a `ViewAction` for each page visit. The vocabulary adds `ViewAction`.
